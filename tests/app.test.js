const DeliveryApp = require('../src/app'); describe('DeliveryApp', () => { test('should create a new order', () => { const app = new DeliveryApp(); const order = app.createOrder({ customer: 'Juan Perez', items: ['Pizza', 'Refresco'] }); expect(order).toHaveProperty('id'); expect(order.status).toBe('pending'); }); test('should get all orders', () => { const app = new DeliveryApp(); app.createOrder({ customer: 'Test' }); expect(app.getOrders()).toHaveLength(1); }); });
